import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['media.dodostatic.com','s3-alpha-sig.figma.com','media.dodostatic.net','cdn.dodostatic.net'],
    
  },
};

export default nextConfig;
